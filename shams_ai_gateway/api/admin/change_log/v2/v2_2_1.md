## Version 2.2.1

### Multi-Tool Orchestration & Report Polling

- **Multi-tool orchestration** for `run_python_code` — call `tools.get_documents()`, `tools.generate_report()` inside Python sandbox for 80-95% token savings
- **Prepared report polling** with exponential backoff — background reports automatically polled until ready (no more manual retries)
- **Dual authentication** support — OAuth 2.0 Bearer tokens and API key/secret for STDIO clients
- **Plugin state fix** — custom tools now properly respect disabled plugin state
- **Data compatibility** — frappe._dict to plain dict conversion for pandas DataFrame compatibility
